#include<stdio.h>
int main(){
    int a = 5;
    printf("%p",&a); //& give the memory address of the variable
                     //%p format specifier of the memory address
                     //always give different address then we need to store address of the variable then we need pointers 
                     //pointer also have a address
    return 0;
}